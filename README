This repository contains the FinalProject.tar.gz file
for the last project for CSCE-350 class
that can be downloaded and ran.  Just select the
.tar.gz file the click on 'View Raw' to download
it.  The directory contains code for different sorts
and experimental data that compares each of them.  
Just download the compressed directory and there are 
further directions within the directory.
